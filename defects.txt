При выполнении работы найдены и исправлены следующие ошибки:

Файл index.html.
1. Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".
2. Опечатка в тексте: "Основныые Интрументы" → исправлено на "Основные Инструменты".
3. Лишний закрывающий тег </p> после строки с классом subtitle — удалён.
4. Неправильное использование <br> внутри тега <p> — заменено на одиночный <br>.

Файл cmd.html.
1. В подвале указан год "202 4" с пробелом — исправлено на "2024".
2. Команда DIR описана как "изменить каталог", а CD — как "отображение файлов". Описания перепутаны — исправлены.
3. Указана несуществующая команда "MAKEDIR" — заменена на "MKDIR".
4. Описание DATE и TIME перепутаны: DATE относится к дате, TIME — ко времени.
5. Тег <title> содержит общее название "Portal for QA" — заменён на "Командная строка Windows".

Файл mac.html.
1. Опечатка в заголовке: "MaOS" → исправлено на "macOS".
2. Перепутаны описания команд mv и mkdir: mv относится к перемещению, mkdir — к созданию папок.
3. Опечатка: "расфшифровывается" → "расшифровывается"; также исправлено ложное утверждение, что mkdir может перемещать или удалять файлы.
4. В подвале использована точка после года ("2024.") вместо запятой — исправлено на "2024, SkyPro".

Файл git.html
1. Описаны не команды Git (ls, cd, pwd и др.), а команды терминала — заменены на настоящие команды Git (git init, git add, git commit и т.д.).
2. Опечатка: "расфшифровывается" → исправлено на "расшифровывается".
3. Описание команды mv содержит ошибку: утверждается, что она может "удалять файлы", что неверно — она только перемещает/переименовывает.
4. Ссылка в футере ведёт на mac.html вместо ../index.html; также опечатка в слове "Вернуся" → "Вернуться".
5. В подвале указан год "202 4" с пробелом — исправлено на "2024".
